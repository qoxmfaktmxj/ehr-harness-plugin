-- 급여 항목 테이블
CREATE TABLE TPAY_ITEM (
  PAY_CD     VARCHAR2(10) NOT NULL,
  PAY_NM     VARCHAR2(100),
  PAY_AMT    NUMBER(15,2) DEFAULT 0,
  CONSTRAINT PK_TPAY_ITEM PRIMARY KEY (PAY_CD)
);
COMMENT ON TABLE TPAY_ITEM IS '급여 항목';
